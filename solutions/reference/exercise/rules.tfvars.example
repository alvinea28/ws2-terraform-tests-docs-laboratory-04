# This corrected input is a provider-mocked fixture, not an Azure plan.
security_rules = {
  web-https = {
    priority                   = 100
    direction                  = "Inbound"
    access                     = "Allow"
    protocol                   = "Tcp"
    source_address_prefix      = "10.42.1.0/24"
    destination_address_prefix = "VirtualNetwork"
    destination_port_range     = "443"
  }
}
